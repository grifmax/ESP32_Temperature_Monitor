# ESP32 Temperature Monitor

Проект для мониторинга температуры с использованием микроконтроллера ESP32 DevKit v1, датчиков DS18B20 и OLED-дисплея. Устройство поддерживает до 10 датчиков температуры с индивидуальными настройками, подключается к Wi-Fi, отображает данные на экране и отправляет уведомления через Telegram и MQTT.

## Основные функции

- 🌡️ **Множественные датчики** - поддержка до 10 датчиков DS18B20 с индивидуальными настройками
- 📺 **Отображение данных** на OLED-дисплее 128x32 (SSD1306)
- 📱 **Управление через Telegram** - полный набор команд для управления устройством и датчиками
- 🌐 **Веб-интерфейс** для настройки и мониторинга с графиками истории
- 📡 **MQTT интеграция** для подключения к системам умного дома
- 🔔 **Система оповещений** с индивидуальными порогами для каждого датчика
- 🎯 **Режим стабилизации** с отслеживанием колебаний температуры
- 📊 **История температуры** с сохранением в SPIFFS (до 288 записей)
- 🔋 **Мониторинг батареи** (опционально)
- 🔊 **Зуммер** для звуковых уведомлений
- ⚡ **Управление питанием WiFi** для экономии энергии
- 🛡️ **Watchdog Timer** для защиты от зависаний
- 🔄 **Асинхронная обработка** Telegram и MQTT в отдельных задачах

## Используемое оборудование

- **Микроконтроллер**: ESP32 DevKit v1
- **Датчики температуры**: DS18B20 (OneWire) - до 10 штук
- **Экран**: OLED 0.91" 128x32 SSD1306 (I2C)
- **Зуммер**: пьезоэлектрический зуммер
- **Кнопка**: тактовая кнопка для управления
- **Батарея**: 18650 через TP4056 (опционально, мониторинг через GPIO 34)

## Подключение компонентов (ESP32 DevKit v1)

| Компонент | Пин ESP32 | Описание |
|-----------|-----------|----------|
| DS18B20 Data | GPIO 4 | Датчики температуры (OneWire, до 10 штук на одной шине) |
| OLED SDA | GPIO 21 | I2C Data (стандартный I2C) |
| OLED SCL | GPIO 22 | I2C Clock (стандартный I2C) |
| Battery Monitor | GPIO 34 | Мониторинг батареи (ADC1, только чтение) |
| Button | GPIO 15 | Кнопка управления |
| Buzzer | GPIO 13 | Зуммер |

**Примечание**: Каждый DS18B20 требует подтягивающего резистора 4.7 кОм между Data и VCC. Все датчики подключаются параллельно к одной шине OneWire.

## Установка и настройка

### Требования

- PlatformIO IDE или VS Code с расширением PlatformIO
- ESP32 DevKit v1
- Библиотеки (устанавливаются автоматически через PlatformIO):
  - OneWire
  - DallasTemperature
  - U8g2
  - UniversalTelegramBot
  - ESPAsyncWebServer
  - ArduinoJson
  - AsyncTCP
  - PubSubClient (MQTT)

### Шаги установки

1. **Клонирование репозитория**:
   ```bash
   git clone https://github.com/grifmax/ESP32_Temperature_Monitor.git
   cd ESP32_Temperature_Monitor
   ```

2. **Настройка конфигурации** (опционально):
   - Откройте `src/config.h`
   - Настройте пины при необходимости (по умолчанию для ESP32 DevKit v1)
   - Настройки Wi-Fi, Telegram и MQTT можно задать через веб-интерфейс

3. **Компиляция и загрузка**:
   ```bash
   pio run -t upload
   ```
   Или используйте PlatformIO IDE для загрузки прошивки.

4. **Загрузка файлов веб-интерфейса**:
   ```bash
   pio run -t uploadfs
   ```

5. **Первоначальная настройка**:
   - При первом запуске устройство создаст точку доступа "ESP32_Thermo" (пароль: 12345678)
   - Подключитесь к этой сети и откройте веб-интерфейс (обычно http://192.168.4.1)
   - Настройте Wi-Fi, Telegram и MQTT через веб-интерфейс
   - Настройте датчики температуры с индивидуальными параметрами

### Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите Bot Token
3. Узнайте свой Chat ID (можно использовать [@userinfobot](https://t.me/userinfobot))
4. Введите эти данные в веб-интерфейсе (раздел "Telegram")

### Настройка MQTT (опционально)

1. Настройте MQTT брокер (например, Mosquitto, Home Assistant, Node-RED)
2. Введите данные сервера в веб-интерфейсе:
   - Сервер MQTT (IP или доменное имя)
   - Порт (обычно 1883 для незашифрованного, 8883 для TLS)
   - Логин и пароль (если требуется)
   - Топики для публикации данных:
     - `home/thermo/status` - статус устройства и метрики
     - `home/thermo/control` - команды управления
     - `home/thermo/alarms` - оповещения о тревогах
   - Тип безопасности (none, TLS)

## Режимы работы устройства

Устройство поддерживает 4 глобальных режима работы:

1. **Локальный режим** (`MODE_LOCAL`)
   - Только локальный мониторинг
   - Wi-Fi включается только при нажатии кнопки или через веб-интерфейс
   - Максимальная экономия энергии
   - Данные не отправляются в сети

2. **Режим мониторинга** (`MODE_MONITORING`)
   - Постоянное подключение к Wi-Fi
   - Отправка данных в MQTT и Telegram при изменении температуры
   - Полный мониторинг всех датчиков

3. **Режим оповещения** (`MODE_ALERT`)
   - Мониторинг с оповещениями при превышении порогов
   - Использует глобальные настройки оповещений (для обратной совместимости)

4. **Режим стабилизации** (`MODE_STABILIZATION`)
   - Использует глобальные настройки стабилизации (для обратной совместимости)

## Режимы работы термометров

Каждый датчик может работать в одном из трех режимов независимо от других:

1. **Режим мониторинга** (`monitoring`)
   - Отправка данных при изменении температуры на заданную уставку
   - Настраиваемая уставка изменения температуры (по умолчанию 1.0°C)
   - Отправка в MQTT и Telegram

2. **Режим оповещения** (`alert`)
   - Мониторинг с оповещениями при выходе за пороги
   - Индивидуальные пороги минимума и максимума для каждого датчика
   - Звуковые и Telegram уведомления
   - Настраиваемое включение/выключение зуммера

3. **Режим стабилизации** (`stabilization`)
   - **Новая логика**: отслеживание колебаний температуры, а не конкретной целевой температуры
   - Устройство отслеживает колебания температуры в течение заданного периода
   - Стабилизация считается достигнутой, если колебания не превышают допуск
   - После стабилизации отслеживаются отклонения от стабилизированной температуры
   - Параметры:
     - **Допуск колебаний** (tolerance) - максимальный разброс температуры за период (по умолчанию ±0.1°C)
     - **Порог тревоги** (alertThreshold) - отклонение от стабилизированной температуры для срабатывания тревоги (по умолчанию 0.2°C)
     - **Длительность наблюдения** (duration) - время для определения стабилизации (по умолчанию 10 минут)

## Команды управления через Telegram

**Примечание**: Все команды можно отправлять как со слэшем (`/help`), так и без него (`help`). Команды не чувствительны к регистру.

### Информационные команды

- `/start` или `/help` - показать справку по всем командам
- `/status` или `/temp` - текущая температура (все датчики)
- `/sensors` - список всех датчиков с их адресами и температурами
- `/info` - подробная информация об устройстве (температура, IP, время работы, Wi-Fi, режим, MQTT)
- `/mode` - текущий режим работы устройства с подробным описанием
- `/wifi` - информация о WiFi подключении (SSID, IP, RSSI, канал, MAC)
- `/mqtt` - статус MQTT подключения

### Управление режимами работы устройства

- `/mode_local` - переключить в локальный режим (только мониторинг)
- `/mode_monitoring` - переключить в режим мониторинга (с отправкой в MQTT/Telegram)
- `/mode_alert` - переключить в режим оповещения
- `/mode_stabilization` - переключить в режим стабилизации температуры

### Настройка оповещений (глобальные, для обратной совместимости)

- `/alert_set <min> <max> [buzzer]` - установить пороги температуры
  - Пример: `/alert_set 10 30 1` - минимум 10°C, максимум 30°C, зуммер включен
  - `buzzer`: 1 - включен, 0 - выключен (по умолчанию 1)
- `/alert_get` - получить текущие настройки оповещений

### Настройка стабилизации (глобальные, для обратной совместимости)

- `/stab_set [tolerance] [alert] [duration_min]` - установить параметры стабилизации
  - Пример: `/stab_set 0.1 0.2 10` - допуск ±0.1°C, порог тревоги 0.2°C, длительность 10 минут
  - `tolerance` - допуск колебаний (по умолчанию 0.1°C)
  - `alert` - порог тревоги (по умолчанию 0.2°C)
  - `duration_min` - длительность наблюдения в минутах (по умолчанию 10)
- `/stab_get` - получить текущие настройки стабилизации

### Управление термометрами

- `/sensor_list` - список всех термометров с их настройками
- `/sensor_name <n> <имя>` - переименовать термометр номер n
  - Пример: `/sensor_name 0 Кухня`
- `/sensor_mode <n> <режим>` - установить режим работы термометра
  - Режимы: `monitoring`, `alert`, `stabilization`
  - Пример: `/sensor_mode 0 alert`
- `/sensor_alert <n> <min> <max> [buzzer]` - установить пороги оповещения для термометра
  - Пример: `/sensor_alert 0 10 30 1`
- `/sensor_stab <n> <tol> <alert> <min>` - настройки стабилизации для термометра
  - Пример: `/sensor_stab 0 0.1 0.2 10` - допуск 0.1°C, порог 0.2°C, длительность 10 минут

### Управление дисплеем

- `/display_on` - включить дисплей (показать температуру)
- `/display_off` - выключить дисплей
- `/display_temp` - показать экран с температурой
- `/display_info` - показать информационный экран

### Управление зуммером

- `/buzzer_test` - протестировать зуммер (короткий сигнал)

### Системные команды

- `/reboot` - перезагрузить устройство (с задержкой 2 секунды для отправки подтверждения)

## Веб-интерфейс

Устройство предоставляет полнофункциональный веб-интерфейс для настройки и мониторинга:

### Главная страница (`/`)

- **Отображение текущей температуры** всех датчиков
- **График истории температуры** с возможностью масштабирования (Chart.js)
- **Статус подключений**: Wi-Fi, MQTT, Telegram
- **Информация об устройстве**: IP адрес, время работы, RSSI
- **Автоматическое обновление** данных каждые 5 секунд

### Страница настроек (`/settings.html`)

- **Настройка Wi-Fi**: SSID, пароль, сохранение в NVS
- **Настройка Telegram**: Bot Token, Chat ID
- **Настройка MQTT**: сервер, порт, логин, пароль, топики, тип безопасности
- **Управление режимами работы**: переключение глобального режима
- **Настройка термометров**:
  - Список всех обнаруженных датчиков
  - Переименование датчиков
  - Включение/выключение датчиков
  - Коррекция температуры (поправка)
  - Выбор режима работы (monitoring, alert, stabilization)
  - Настройка уставки изменения для режима мониторинга
  - Настройка порогов оповещения
  - Настройка параметров стабилизации
  - Управление отправкой в сети
  - Управление зуммером для каждого датчика
- **Управление дисплеем**: включение/выключение, переключение экранов
- **Тестирование**: тест MQTT, тест Telegram, тест зуммера

Доступ к веб-интерфейсу:
- При подключении к Wi-Fi: `http://<IP_адрес_устройства>`
- В режиме точки доступа: `http://192.168.4.1`

## Управление кнопкой

Кнопка на устройстве поддерживает различные действия:

- **Короткое нажатие**: Переключение между датчиками на дисплее (циклическое переключение)
- **Двойное нажатие**: Включение/выключение дисплея
- **Длинное нажатие** (>2 сек): Перезагрузка устройства

В локальном режиме короткое нажатие также включает Wi-Fi для доступа к веб-интерфейсу.

## MQTT интеграция

### Топики

- **`home/thermo/status`** (публикация) - статус устройства и метрики
  - Формат: JSON с полями `uptime`, `temperature`, `ip`, `rssi`, `sensors`
  - Публикуется каждые 60 секунд при подключении
- **`home/thermo/control`** (подписка) - команды управления устройством
- **`home/thermo/alarms`** (публикация) - оповещения о тревогах

### Формат сообщений

**Статус** (`home/thermo/status`):
```json
{
  "uptime": 3600,
  "temperature": 25.5,
  "ip": "192.168.1.100",
  "rssi": -65,
  "sensors": [
    {
      "address": "28FF1234567890AB",
      "name": "Кухня",
      "temperature": 25.5,
      "mode": "monitoring"
    }
  ]
}
```

### Безопасность

Поддерживаются следующие типы безопасности:
- **none** - без шифрования (порт 1883)
- **TLS** - с шифрованием (порт 8883)

## История температуры

- Сохранение до **288 записей** (24 часа при записи каждые 5 минут)
- Автоматическое сохранение в SPIFFS
- Загрузка истории при старте устройства
- История хранится отдельно для каждого датчика
- Отображение в веб-интерфейсе с графиками

## Структура проекта

```
ESP32_Temperature_Monitor/
├── src/                          # Исходный код
│   ├── main.cpp                  # Главный файл, инициализация и основной цикл
│   ├── config.h                  # Конфигурация (пины, настройки по умолчанию)
│   ├── sensors.cpp/h             # Работа с датчиками температуры (OneWire, DallasTemperature)
│   ├── display.cpp/h             # Управление OLED дисплеем (U8g2)
│   ├── tg_bot.cpp/h              # Telegram бот (обработка команд, отправка сообщений)
│   ├── mqtt_client.cpp/h         # MQTT клиент (PubSubClient, асинхронная обработка)
│   ├── web_server.cpp/h          # Веб-сервер (ESPAsyncWebServer, API endpoints)
│   ├── operation_modes.cpp/h     # Режимы работы устройства
│   ├── buzzer.cpp/h              # Управление зуммером
│   ├── temperature_history.cpp/h # История температуры с сохранением в SPIFFS
│   ├── time_manager.cpp/h        # Управление временем (NTP)
│   └── wifi_power.cpp/h          # Управление питанием WiFi
├── data/                         # Файлы веб-интерфейса (загружаются в SPIFFS)
│   ├── index.html                # Главная страница с графиками
│   ├── settings.html             # Страница настроек
│   ├── script.js                 # JavaScript для главной страницы
│   ├── settings.js               # JavaScript для страницы настроек
│   └── style.css                 # Стили CSS
├── platformio.ini                # Конфигурация PlatformIO
├── partitions.csv                # Таблица разделов Flash памяти
└── README.md                     # Документация
```

## Технические детали

- **Платформа**: ESP32 (Espressif)
- **Framework**: Arduino
- **Файловая система**: SPIFFS (для веб-интерфейса и истории)
- **Хранилище настроек**: 
  - SPIFFS (JSON файлы) - основные настройки
  - NVS (Non-Volatile Storage) - критичные настройки (Wi-Fi, Telegram) для быстрого доступа
- **Партиции**: 
  - NVS: 20 KB
  - OTA: 8 KB
  - App: 1.7 MB
  - SPIFFS: 2.3 MB
- **Скорость мониторинга**: 115200 baud
- **Интервал чтения температуры**: 10 секунд
- **Интервал отправки метрик MQTT**: 60 секунд
- **Watchdog Timer**: 30 секунд (защита от зависаний)
- **Максимальное количество датчиков**: 10
- **Размер истории**: 288 записей на датчик

## Архитектура и производительность

- **Асинхронная обработка**: Telegram и MQTT обрабатываются в отдельных FreeRTOS задачах
- **Неблокирующие операции**: все сетевые операции асинхронные
- **Кеширование настроек**: настройки датчиков кешируются и перезагружаются каждые 30 секунд
- **Оптимизация памяти**: использование статических буферов, ограничение размера истории
- **Управление питанием**: автоматическое управление WiFi для экономии энергии

## Устранение неполадок

### Устройство не подключается к Wi-Fi

- Проверьте правильность SSID и пароля в веб-интерфейсе
- Убедитесь, что устройство в зоне действия сети
- Попробуйте перезагрузить устройство
- Проверьте Serial Monitor для отладочной информации

### Telegram бот не отвечает

- Проверьте правильность Bot Token и Chat ID в веб-интерфейсе
- Убедитесь, что устройство подключено к интернету
- Проверьте Serial Monitor для отладочной информации
- Используйте команду `/start` для проверки связи

### MQTT не подключается

- Проверьте правильность адреса сервера и порта
- Убедитесь, что логин и пароль правильные (если требуется)
- Проверьте тип безопасности (none/TLS)
- Проверьте, что брокер доступен из сети устройства
- Используйте тест MQTT в веб-интерфейсе

### Датчик не показывает температуру

- Проверьте подключение DS18B20 (Data к GPIO 4)
- Убедитесь в наличии подтягивающего резистора 4.7 кОм между Data и VCC
- Проверьте питание датчика (3.3V или 5V)
- Используйте команду `/sensors` в Telegram для проверки обнаруженных датчиков
- Проверьте, что датчик включен в настройках веб-интерфейса

### Дисплей не работает

- Проверьте подключение I2C (SDA к GPIO 21, SCL к GPIO 22)
- Убедитесь в правильности адреса дисплея (обычно 0x3C)
- Проверьте питание дисплея
- Попробуйте команду `/display_on` в Telegram

### Устройство перезагружается

- Проверьте Serial Monitor для сообщений об ошибках
- Убедитесь, что Watchdog Timer не срабатывает (проверьте логи)
- Проверьте стабильность питания
- Проверьте, не переполняется ли память (размер истории, количество датчиков)

### Настройки не сохраняются

- Убедитесь, что SPIFFS смонтирован (проверьте Serial Monitor)
- Попробуйте перезагрузить устройство
- Проверьте, что есть свободное место в SPIFFS
- Критичные настройки (Wi-Fi, Telegram) сохраняются в NVS и должны работать даже при проблемах с SPIFFS

## Лицензия

Проект распространяется по лицензии MIT.

## Авторы

Разработано для мониторинга температуры с использованием ESP32.

## Поддержка

При возникновении проблем:
1. Проверьте Serial Monitor для отладочной информации (115200 baud)
2. Убедитесь в правильности подключения компонентов
3. Проверьте настройки в веб-интерфейсе
4. Используйте команды Telegram для диагностики
5. Создайте issue в репозитории с описанием проблемы

## Документация

- [README.md](README.md) - основная документация проекта
- [API.md](API.md) - документация REST API
- [CHANGELOG.md](CHANGELOG.md) - история изменений проекта

## Ссылки

- Репозиторий: https://github.com/grifmax/ESP32_Temperature_Monitor
- PlatformIO: https://platformio.org/
- ESP32: https://www.espressif.com/en/products/socs/esp32
